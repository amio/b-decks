{
  "name": "decks-bd",
  "private": true,
  "scripts": {
    "build": "decks=(aeolus-onpremise cd eloquence-cli nothing-everything serverless); for d in ${decks[@]}; do eloc build $d/index.md -o public/$d; done"
  },
  "devDependencies": {
    "eloc": "^1.2.2"
  }
}
